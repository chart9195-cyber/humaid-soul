name: humaid_soul_ui
description: The cognitive reading interface of HUMAID SOUL.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # dart_pdf: ^3.10.0
  # flutter_tts: ^3.8.0
  # sqflite: ^2.3.0
  # path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/dictionaries/
