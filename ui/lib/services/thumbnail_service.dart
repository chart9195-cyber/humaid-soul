// ThumbnailService is currently a stub – thumbnails will be re‑introduced
// once the Syncfusion render API is confirmed across all supported versions.
class ThumbnailService {
  static Future<String?> getThumbnail(String pdfPath) async {
    // Returning null means the library will show the default PDF icon.
    return null;
  }
}
