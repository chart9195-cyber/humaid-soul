/// Placeholder for PDF word extraction using a native library (MuPDF, PDFium, etc.)
/// Will be implemented with real bindings when we add the PDF rendering layer.
pub fn extract_page_words(_pdf_path: &str, _page_num: u32) -> String {
    // Return an empty JSON array for now.
    "[]".to_string()
}
